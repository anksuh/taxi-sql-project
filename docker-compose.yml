services:
  postgres:
    image: postgres:16
    container_name: taxi_pg
    environment:
      POSTGRES_USER: taxi_user
      POSTGRES_PASSWORD: taxi_pass
      POSTGRES_DB: taxi_db
    ports:
      - "5432:5432"
    volumes:
      - taxi_pgdata:/var/lib/postgresql/data

volumes:
  taxi_pgdata:
